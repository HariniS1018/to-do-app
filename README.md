# Flask To-Do App ✅

A simple **To-Do application** built with **Flask** as part of a learning journey.  
The project evolves across multiple versions (`app1.py` → `app5.py`), each adding new features and improvements.

---

## 📂 Project Structure

```
to-do-app/
 ├── app1.py   # First version: basic Flask setup
 ├── app2.py   # Adds simple routes and data
 ├── app3.py   # Introduces task management (add/view)
 ├── app4.py   # Adds edit/delete functionality
 ├── app5.py   # Final version
 └── README.md
```

---

## 🚀 Features (by version)

- **app1.py** → Basic Flask "Hello World"
- **app2.py** → Simple Routing
- **app3.py** → Add and view tasks
- **app4.py** → Edit and delete tasks
- **app5.py** → Persistent storage with database

---

## 🛠️ Tech Stack

- **Backend:** Flask (Python)  
- **Database (final version):** PostgreSQL  

---

## ⚙️ Setup Instructions

1. Clone the repository:
   ```bash
   git clone https://github.com/HariniS1018/to-do-app.git
   cd flask-todo-app
   ```

2. Install dependencies:
   ```bash
   pip install flask
   ```

3. Run any version (example: `app3.py`):
   ```bash
   python app3.py
   ```
---